[color]
	ui = true
[user]
	name = Sam Kaplan
	email = stkaplan@gmail.com
[alias]
	st = status
	ci = commit
	br = branch
	co = checkout
    serve = !git daemon --reuseadder --verbose --base-path=. --export-all ./.git
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(cyan)<%an>%Creset' --abbrev-commit --date=relative
	unstage = reset HEAD --
	staged = diff --cached
    out = log --oneline origin..HEAD
    in = log --oneline HEAD..origin
    aliases = !git config --get-regexp 'alias.*' | colrm 1 6 | sed 's/[ ]/ = /'
[color "diff"]
	new = cyan bold
	old = red bold
	meta = green bold
	frag = yellow bold
[core]
	excludesfile = ~/.gitignore
[web]
	browser = firefox
